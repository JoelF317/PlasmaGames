query launchList($loc: String!){
  launchesPast(find: {
      site_id: $loc
    }
  ){
    id
    mission_name
    links {
      flickr_images
      mission_patch_small
    }
    rocket{
      rocket_name
    }
    launch_date_utc
    launch_site{
      site_name_long
      site_id
    }
  }
}
